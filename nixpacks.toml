[phases.setup]
aptPkgs = ["postgresql-client", "libpq-dev"]

[phases.install]
cmds = ["echo 'Installing PostgreSQL PHP extensions - Build 2025-11-15'"]

[phases.build]
cmds = ["echo 'Force rebuild - v3.1.1'"]

[start]
cmd = "frankenphp run --config /app/Caddyfile"
